# Development Environment Variables
# Copy this file to .env.local and fill in your values

# API Configuration
VITE_API_URL=http://localhost:3000/api

# SecureLS Encryption Key (must be 32+ characters for production)
VITE_SECURE_LS_KEY=development-encryption-key-32-chars-minimum

VITE_APP_ENV=development

# Other configuration
VITE_APP_NAME=NAIS Application
VITE_APP_VERSION=1.0.0

# Note: In production (AWS App Runner), these will be replaced by runtime configuration
# Set environment variables in AWS App Runner console:
# - API_URL (instead of VITE_API_URL)
# - SECURE_LS_KEY (instead of VITE_SECURE_LS_KEY)